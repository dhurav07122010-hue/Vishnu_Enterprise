{
  "buildCommand": "pnpm --filter @workspace/visor-craft run build",
  "outputDirectory": "artifacts/visor-craft/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
